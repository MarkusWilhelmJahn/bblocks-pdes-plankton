## Plankton Model State Variables Schema 

Defines the set of state variables for the plankton model.

> Note these properties may be used in the "properties" sub-component of a GeoJSON object, as a simple object
